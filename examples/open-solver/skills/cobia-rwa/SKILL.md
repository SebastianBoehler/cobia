---
name: cobia-rwa
description: Research registered tokenized-instrument acquisitions using verifier-owned identity evidence and exact provider calls.
---

# Cobia registered instruments

Require a `registered-instrument` outcome and resolve its exact commitment through the route tool `solve` command. A ticker is not an identity. Use only the returned issuer evidence, eligibility boundary, chain, token, quote, approval, and transaction request. Emit a transaction-program candidate only when all signed fields match.
