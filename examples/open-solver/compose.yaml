services:
  solver:
    build:
      context: ../..
      dockerfile: examples/open-solver/Dockerfile
    restart: unless-stopped
    init: true
    env_file: .env
    environment:
      CODEX_HOME: /auth/codex
    volumes:
      - codex-auth:/auth/codex
      - ./codex/config.toml:/auth/codex/config.toml:ro
      - solver-state:/var/lib/cobia-solver

volumes:
  codex-auth:
  solver-state:
