{
  "name": "cobia",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --filter @cobia/web dev",
    "contracts:test": "scripts/forge.sh test",
    "executor:plan": "pnpm --filter @cobia/web exec tsx scripts/prepare-agent-executor-deployment.ts",
    "env:dev": "node apps/web/scripts/seed-dev-env.mjs",
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "5.6.1",
    "tsx": "4.23.11",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
